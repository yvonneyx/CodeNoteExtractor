'use strict';

/**
 * Add two numbers
 * @remarks
 * This is a remark for add function to explain more about it.
 * @param a the first num
 * @param b the second num
 * @returns Sum of two numbers
 * @example
 * ```ts
 * import { add } from 'my-ts-lib';
 * const sum = add(1, 2); // 3
 * ```
 */
function add(a, b) {
    return a + b;
}

exports.add = add;
